## MapCanvas

**Purpose:** Stateful editable xyflow canvas — renders systems, connections, and free-standing notes, applies realtime events, orchestrates optimistic + reconcile mutations against the map API, and drives the sidebar inspector.
**File:** `src/components/map/MapCanvas.tsx`

> Two node types share the canvas: `system` (`SystemNode`) and `note` (`MapNoteNode`). Because `ap_map_system.id` and `ap_map_note.id` are independent identity sequences and collide numerically, **note xyflow node ids are namespaced** `note:<id>` (`noteNodeId` helper) while the real `ap_map_note.id` stays on `node.data.id`. Note handlers branch on `node.type === 'note'` and read the id from `data.id`. The node state is the union `CanvasNode = Node<SystemNodeData> | Node<MapNoteNodeData>`.

### Props
| Prop | Type | Required | Description |
|---|---|---|---|
| data | MapViewData | yes | Initial map + systems + connections + signatures (from `loadMapForView`). |
| stats | Record<number, SystemStatsSummary> | yes | Precomputed 24h kill stats keyed by EVE system id (load-time seed for `stats` state; live-added systems backfill via `fetchSystemData`). |
| intel | Record<number, SystemIntelSummary> | yes | Read-side integration intel keyed by EVE system id (load-time seed for `intel` state; live-added systems backfill via `fetchSystemData`). |
| structures | Record<number, StructureIntel[]> | yes | Manual structure intel keyed by EVE system id (load-time seed for `structures` state; live-added systems backfill via `fetchSystemData`, own CRUD updates in place; no realtime echo). |
| systemNotes | Record<number, SystemNote[]> | yes | Global system notes keyed by EVE system id (load-time seed for `systemNotes` state; same backfill/CRUD/no-realtime model as `structures`). |
| settings | MapSettings | yes | Editable map metadata + behaviour toggles (from `loadMapSettings`), seeds the `MapSettingsDialog`. |
| canManage | boolean | yes | Derived `canManageMap` — passed to `MapSettingsDialog` where it reveals the manager-only Roles & Permissions tab. UI gating only; surfaces are re-checked server-side. |
| capabilities | MapCapability[] | yes | The delegated map capabilities the viewer holds (`resolveMapCapabilities`) — a manager holds all, a delegated corp title a subset. Reveals the toolbar "Audit log" button + `MapAuditDialog` (`audit_view`) and, via `MapSettingsDialog`, each management tab. UI gating only; every endpoint re-checks server-side. |
| liveShares | LiveShareBadge[] | yes | Share links currently publishing this map (`loadLiveShareBadges`). Seeds the header `MapShareIndicator`, which every viewer sees regardless of capability. |
| travelAnimation | boolean | yes | The viewer's per-account connection-travel-animation toggle. When true, mounts the `TravelBridge` that plays the moving-dot effect on pilot jumps. |
| signatureIndicators | SignatureIndicatorPrefs | yes | The viewer's resolved stale/unscanned indicator prefs (effective threshold + the two toggles). Feeds `MapSignatureIndicatorProvider`. |
| viewerCharacterIds | number[] | yes | The viewer's account character ids; passed to `SignaturePasteHotkey` for the CTRL+V fast-paste location check. |
| viewerCharacters | { id: number; name: string }[] | yes | The viewer's active characters (id + name); passed to `MapActiveCharProvider` to seed the active-character selector. |
| mainCharacterId | number \| null | yes | The account's main character id (fallback when no character is explicitly picked), or null. |
| sessionCharacterId | number | yes | The signed-in (session) character id. Personal chains render only for this owner — mirroring `loadMapForView`, so a foreign personal chain arriving over realtime never renders. |
| routePrefs | RoutePrefs | yes | Per-account route-planner settings (routes-module); seeds `RoutePlannerModule`. |
| routeDestinations | RouteDestinationView[] | yes | The account's saved route destinations (routes-module). |
| mapLayout | MapLayoutConfig \| null | no | The viewer's saved per-account dashboard layout (map-layout-builder). Seeds the live layout state, run through `migrateLayout` (pre-v2 → singleton groups) → `dedupeGroups` (heal duplicated members) → `ensurePanelsPlaced` (forward-compat); `null` ⇒ `DEFAULT_MAP_LAYOUT`. |

### Renders
A free-form dashboard (the page scrolls). A full-width toolbar row sits above a `MapLayoutGrid`: the map name + meta (`type · scope · N systems`, live from `viewData`) plus the `MapShareIndicator` badge on the left, and a right-aligned cluster of ghost buttons — `ActiveCharSelector` (compact select with User icon; always visible: shows a dimmed "No characters" label with an explanatory tooltip when none are tracked, or the character select with a tooltip when characters are available), `PilotRosterButton`, `SystemOverlayButton` (pops the read-only in-game system overlay into a Document Picture-in-Picture window), a "Panels" dropdown, "Add system", "Map info", "Settings" — on the right. The page no longer renders a separate heading — the toolbar row is the only name display. (react-grid-layout `Responsive`). Every grid cell is a `MapPanelGroup` — a draggable-header card holding one or more member panels as tabs (a single-member group is an untabbed panel) — wrapping the `ReactFlow` canvas or a sidebar/signature module and placed by the saved layout. Panels: `canvas`, `signatures`, `inspector`, `route`, `intel`, `structure`, `systemNotes`, `killStats`, `systemGraph`, `systemKillboard`, `tags` (auto-tag "next available", hidden when the map runs no scheme), `thera`. The canvas panel uses `contentClassName="min-h-0 flex-1 overflow-hidden p-0"` so ReactFlow fills a clean, definite-height cell; it stacks a `ChainTabStrip` above the canvas area, which is **either** the free-canvas ReactFlow (the "All" tab — default) with the floating "Remove N" button and the non-blocking `SubchainDeletePrompt` overlays, **or** a `ChainCanvas` rendering the active chain's generated tree; the `MapContextMenu` is mounted beside them and serves both modes. The default arrangement (`DEFAULT_MAP_LAYOUT`) is a two-column look: tall canvas top-left, full-width signatures below, the info modules stacked right. A `MapInfoDialog`, `MapSettingsDialog`, and `AddSystemDialog` are mounted (closed by default) inside the presence provider.

### Behaviour & Interactions
- `viewData` is seeded from `data` and mutated by both realtime events and local optimistic patches; the canvas is the single source of canvas-render state.
- Realtime apply: `useRealtimeEvents` (not the old `lastEvent` state) filters inbound envelopes to `task === 'mapUpdate'`, validates with `mapUpdateLoadSchema`, dedupes by `eventId`, and folds the payload via `setViewData(prev => applyEvent(prev, payload))`. Because every envelope is delivered exactly once (not coalesced through a `useState` slot), a same-tick burst — e.g. a wormhole jump's `system.added` + `connection.create` + trailing `characterUpdate` — applies all of them in order; the functional update composes so the connection is never dropped. (This closes the "new system shows but its connection is missing until refresh" bug.)
- The same realtime callback keeps `liveShares` current: `share.created` appends a badge (id-checked, so a double delivery can't duplicate it) and `share.revoked` drops one. These kinds carry no canvas state, so `applyEvent` returns `state` untouched for them.
- `appliedEventIds` ref dedupes the initiating tab's own realtime echo by `eventId`.
- `runOptimistic(optimisticPayload, run)` snapshots `viewData`, applies the payload through `applyEvent` locally, fires the network call, and either records the returned `eventId` (success) or restores the snapshot **and triggers `resync()`** (failure). Used for PATCH / DELETE.
- `awaitServer(run)` posts and, on success, applies the server's `MapEventPayload` through `applyEvent` and records its `eventId`; on failure it triggers `resync()`. Used for POST.
- `resync()` — self-heal failsafe. Refetches the authoritative snapshot via `fetchMapSnapshot(data.map.id)` (GET `/api/map/{mapId}`), clears `appliedEventIds`, and replaces `viewData`. Guarded by a `resyncInFlight` ref so a burst of triggers collapses into one refetch. A rollback alone can't fix every drift — e.g. a signature orphaned on the client after its connection (and the DB row, via `ON DELETE CASCADE`) was deleted; resync reconciles against DB truth. Triggered on **mutation failure** (rollback can't recover) **and on socket reconnect** (see below).
- `useReconnectResync(resync)` — calls `resync()` when the realtime socket reopens after a `degraded`/`closed` gap, recovering events committed during the disconnect (the SharedWorker resumes only NEW events on reconnect). Does **not** fire on the initial mount-open — the page-load snapshot is already fresh. The `resyncInFlight` guard means a reconnect during an in-flight error-resync collapses safely.
- `onBulkPaste(payloads)` loops the N committed payloads, registers every `eventId` in `appliedEventIds`, and folds each through `applyEvent` in commit order — the bulk equivalent of `awaitServer`. Shared by signature paste, import, Thera sync, subchain delete, **and manual add** (the add returns the new system + its auto gate links). Defined just after `awaitServer` so the earlier `onAddSystem` can depend on it. It also sets `pasteFlash` from the batch's `signature.create` / `signature.update` payloads (id → `'created'`/`'updated'`) and clears it after 2.5s, driving the signature-panel sonar-ping highlight. Because only the local-apply path runs this, a paste flashes for the pilot who did it, not for remote viewers (their realtime envelopes fold straight through `applyEvent`).
- **Signature hydration on `system.added`.** Signatures no longer ride the event (the full-sig payload breached the 8 KB `pg_notify` ceiling and rolled the insert back). `hydrateAddedSystems(payloads)` scans a just-applied batch for `system.added` and calls `hydrateSignatures(mapSystemId)` for each — a `fetchSystemSignatures` GET whose result is **upsert-by-id** merged into `viewData.signatures`. Fired from all three fold sites (realtime handler, `awaitServer`, `onBulkPaste`) *outside* the `setViewData` updater, deduped by the `sigHydrateInFlight` ref. Keyed on the event (not a `viewData` diff) because re-add reuses the same `ap_map_system.id`, so a diff-and-dedupe effect would never refire. Upsert is race-tolerant — a `signature.create` arriving mid-fetch is never clobbered.
- Nodes are managed in xyflow's "controlled" pattern: `nodes` is a `useState`, and `onNodesChange` applies xyflow's `NodeChange[]` through `applyNodeChanges`. This is what makes drag visually smooth — without an `onNodesChange` handler, xyflow's drag/position events have nowhere to land and nodes don't move until release. A render-time sync block (keyed on `{ systems, notes, selectedSystemIds, selected, intel }`, compared by reference) rebuilds the single `nodes` array as `[...systemNodes, ...noteNodes]`, preserving each node's `dragging` position and xyflow-internal fields (`measured`, etc.) by spreading the existing entry (looked up by id — `noteNodeId(n.id)` for notes) — without that, every sync would re-measure and nodes would flicker out. System nodes carry `selected: selectedSystemIds.has(s.id)` + `onAliasOrTagCommit`; note nodes carry `selected: selected?.kind === 'note' && selected.id === n.id` + `onOpen: onOpenNote` + `draggable: !n.locked`. `selected` is in the sync key because the note halo is driven by the inspector ref (notes have no multi-select set).
- Notes: free-standing `MapNoteNode`s built from `viewData.notes`. **Create** — the pane context-menu "Add note here" calls `onAddNoteAt(clientX, clientY)`, which converts the cursor point via `screenToFlowPosition`, snaps, and POSTs `addNoteOnServer` (default title "New note") through `awaitServer` (no picker dialog; the user double-clicks the new note to edit). **Move** — `onNodeDragStop` branches on `node.type === 'note'` first: snap the drop and optimistically `updateNoteOnServer` the position via `runOptimistic` — **no collision nudge** (notes may overlap anything). **Edit** — double-click a note → `data.onOpen` → `onOpenNote` selects it (`selected = { kind: 'note', id }`, set cleared) → `NoteInspector`; field edits flow through `onNotePatch` (optimistic `note.updated`). **Delete** — `onNoteRemove` (inspector Remove / context-menu Delete) optimistic `note.deleted`, then clears `selected`. The `note.updated` optimistic payload always carries `title` + the note's current `lastEditedBy*` + `updatedAt` (the authoritative echo overwrites attribution with the real actor), so a position/field edit never blanks "last edited by".
- Scroll wheel: `zoomOnScroll={false}` and `preventScrolling={false}` — the wheel does not zoom the canvas; native browser scroll propagates to the page, scrolling down to the signature panel.
- Viewport persistence: on mount, the stored viewport for this map (`aperture:map:<id>:viewport` in localStorage) is restored via `defaultViewport`; `fitView` is only enabled on first visit (no stored viewport). `onMoveEnd` writes the new viewport after every pan/zoom, and mirrors it into `lastViewportRef` so the free ReactFlow restores the viewport the user left when it remounts after a chain-tab visit.
- **Chain mode (nomadic-chains):** the canvas cell stacks a `ChainTabStrip` ("All" + every visible chain — shared chains plus the session character's own personal chains via `sortChainsForTabs`; realtime-delivered foreign personal chains are filtered out) above the canvas area. The tabs are the mode toggle: "All" renders the free canvas unchanged; a chain tab swaps in `ChainCanvas` (keyed by chain id, so each open refits) with nodes/edges derived per render from `viewData.chains`/`chainMembers` — `buildChainCanvas` (pure, `@/lib/map/chains/view`) runs `layoutChainTree` with `CHAIN_TILE_PARAMS` and the preferred orientation, memoized on members/systems/connections/orientation, so charting into the tab (or a tracked jump) grows the tree live off the ordinary `chain.member.added` fold.
  - The active tab + orientation persist per map in localStorage (`aperture:map:<id>:chainView`); both are display preferences, no schema.
  - Occurrence tiles reuse the `system` node type with the canonical row's data (full SystemNode affordances incl. inline alias/tag edit), xyflow ids `chainId:mapSystemId`, `draggable: false` (the generated layout owns positions). Clicking selects the **canonical** system (`selected` + `selectedSystemIds`), so the inspector and every sidebar module work unchanged; right-click opens the canonical system context menu. Pointer-leaves render as `ChainPointerNode` pills (`selectable: false`); clicking one switches to the target chain's tab (a loop stays put), selects the target system, and centers it via a `ChainFocusRequest`; a pointer into a chain the viewer can't see toasts instead.
  - Tree edges with a live backing connection render through `ConnectionEdge` with the edge id **equal to the connection id**, so edge click/selection and the endpoint context menu drive the canonical connection; a link whose via connection is collapsed/unknown renders as a muted dashed default edge (not selectable).
  - Charting carries chain context: a connection drawn between two occurrences POSTs with `chainId` + `sourceMemberId` (the membership event arrives over realtime); `onAddSystem` with a chain tab open passes `chainId` + `parentMemberId` (the selected occurrence's member, else the chain's root, else the add becomes the root) and anchors the free-canvas position on the parent's canonical spot.
  - Chain lifecycle: `onChainCreate` awaits `createChainOnServer` and activates the new tab; rename/delete are optimistic (`chain.renamed` / `chain.deleted` payloads through `runOptimistic`); deleting the active chain falls back to the All tab. Personal chains are open to anyone; the strip offers shared-chain creation/manage only when `canManage` (server re-checks).
  - `onJumpToSystem` and `handleNavigateToSig` branch on `activeChainIdRef`: in chain mode they focus through a `ChainFocusRequest` instead of the free-canvas `flowInstance` (which is unmounted while a chain tab is open).
- Dashboard layout (map-layout-builder): `layout` state is seeded from the `mapLayout` prop (`DEFAULT_MAP_LAYOUT` when null). `visibleGroups` filters the active breakpoint's `layout.groups[breakpoint]` to groups with at least one non-hidden member and renders one `MapPanelGroup` per grid cell (keyed by group id); a `panelContent(id)` switch returns the active tab's body and `panelHeaderRight(id)` its header actions. The active `breakpoint` state is reported by `MapLayoutGrid` via `onBreakpointChange`, so the rendered cells track the responsive breakpoint (grouping is per-breakpoint). `MapLayoutGrid` receives `layout.layouts` and `handleLayoutChange`, which merges RGL's reported per-breakpoint geometry (`mergeLayouts`, union by item `i` so hidden/unplaced panels keep their slot) into state and **debounce-saves** (`LAYOUT_SAVE_DEBOUNCE_MS` 600 ms) via `setMapLayoutAction`. The **first** `onLayoutChange` (RGL's mount-time normalization) updates state but does not save, so opening a map writes nothing. A tab's close (✕), or a single-member group's hide (✕) button, calls `handleHide` (via `onHideMember`), which removes the panel from its group in every breakpoint (`removePanelFromLayout` — picks a new active tab, re-keys the group + its `layouts[bp]` item when the panel was the anchor id, drops the group + item when it was the sole member), appends the panel id to `layout.hidden`, and saves. Clicking a tab calls `handleSetActiveTab`, which sets that group's `active` (in every breakpoint that holds a group with the id and lists the panel) and saves. **Tab drag (dnd-kit):** the grid is wrapped in `PanelDndContext`; each tab is draggable and each group header a drop target. On drop, `handlePanelDrop(activePanel, overId)` resolves the raw droppable id against the **active breakpoint's** groups and dispatches — same group ⇒ `reorderTab` (`arrayMove` within `members`), different group or a header ⇒ `mergePanelIntoGroup` (removes the panel from its source group, dropping the source group and its `layouts[bp]` item when it empties, then appends it as the target's new active tab). A group's id is always one of its current members (its "anchor"): removing the anchor member from a surviving group re-keys the group and its `layouts[bp]` item to the new active member, so a freed `PanelId` is never left as a dangling group id that a later tear-off/merge could reuse for a second cell with the same key. Both mutate only `groups[breakpoint]` (grouping is per-breakpoint), and save. **Tab tear-off (dnd-kit):** dropping a tab on the open grid surface (rather than a header) fires `MapLayoutGrid`'s `onTearOff` → `tearOffTab(panel, x, y)`, which splits that member into its own new singleton cell at the snapped `x/y` (sized to the panel's `PANEL_MIN`), picking a new active tab in the source group and dropping the source if it empties; the new group/layout-item id is the panel's own `PanelId` (grid ids live in the `PanelId` space), and when the panel is its group's anchor id the source group + its layout item are first re-keyed to a remaining member so the id isn't claimed by two cells. Tearing off a singleton's sole tab has nothing to split, so it just repositions that cell's layout item (a move). Also per-breakpoint; RGL compaction settles any overlap. The toolbar **Panels dropdown** (a `Menu` of one `MenuCheckboxItem` per `PANELS` entry, checked ⇒ visible) calls `handleToggleVisible` to flip a panel in/out of `layout.hidden` — hiding routes through the same group-aware `removePanelFromLayout` as the tab ✕, and re-showing drops the panel from `hidden` and runs `ensurePanelsPlaced` to re-add it as a fresh singleton group at the bottom of each breakpoint (a hidden panel has no preserved slot). The menu's checkbox items stay open across toggles. A "Reset layout" item (`handleResetLayout`) re-seeds state from a `structuredClone(DEFAULT_MAP_LAYOUT)` (clearing `hidden` and restoring the shipped geometry) and saves. **Export / Import layout** (shareable layout files): "Export layout" (`handleExportLayout`) downloads the live `layout` as `aperture-layout.json` (a raw `MapLayoutConfig` blob via an object-URL anchor click). "Import layout" triggers a hidden `<input type="file">` whose `onChange` reads the file and calls `handleImportFile`, which `JSON.parse`s the text, re-validates it at the boundary with `mapLayoutConfigSchema.safeParse` (a bad parse or shape → `toast.error`), runs the result through `dedupeGroups` (heal any duplicated tabbed members) then `ensurePanelsPlaced` (back-fills panels absent from a layout saved by another build), then `setLayout` + `saveLayout` and `toast.success`. The input's value is cleared on every change so re-selecting the same file re-fires. **Panels with no current selection keep their grid slot and render their own empty state** (e.g. "Select a system …") rather than collapsing, since the layout is geometry keyed by panel id; "hidden" (user removed) ≠ "empty" (no selection). Distinct `lg`/`md`/`sm` arrangements are saved together (RGL reports all breakpoints; `mergeLayouts` folds each), and narrowing past a breakpoint swaps to that breakpoint's independent arrangement. The pending debounce is cancelled on unmount. The canvas is a grid item whose header (`.ap-panel-drag`) is the only drag handle, so the ReactFlow body keeps pan/zoom/box-select; ReactFlow v12 auto-fits on cell resize via its own ResizeObserver (no manual `fitView` on resize).
- Drag: `nodesDraggable` is on globally, but each node carries a per-node `draggable: !s.locked` flag (set both in the initial nodes state and in the reconcile sync block) so **locked systems are not draggable** — xyflow honours the per-node `draggable: false` over the canvas-wide setting. A locked node inside a multi-selection is likewise left in place during a group drag (xyflow won't move a non-draggable node), and `commitGroupMove`'s unchanged-position skip means no PATCH is emitted for it. `onNodesChange` updates the position live (so the node follows the cursor). Live dragging snaps to the grid (`snapToGrid` + `snapGrid={[GRID_SIZE, GRID_SIZE]}` from `@/lib/map/placement`). `onNodeDragStop` snaps the drop point, and if it `overlaps` any *other* node (at the tighter `MANUAL_SLOT` gap, so manual drags can pack closer than auto-placement allows), replaces it with `findOpenPosition(snappedDrop, occupiedOthers, MANUAL_SLOT)` (search starts at the drop so the nudge is minimal); the final snapped/nudged coords PATCH `positionX`/`positionY` optimistically through `viewData` (which the reconcile effect then folds back into the nodes state, settling the node into the nudged slot). No write if the snapped/nudged position equals the current one. When the grabbed node is part of a multi-selection (`selectedSystemIds.size > 1 && has(node.id)`), `onNodeDragStop` instead delegates to `commitGroupMove()` and returns. **Notes** take a separate branch: notes are never in `selectedSystemIds`, but xyflow still group-drags every node it selected (e.g. a box-select over several notes), so the note branch reads the live store (`flowInstance.getNodes()`) and commits *every* selected note — not just the grabbed one — otherwise the un-committed notes snap back to their `viewData` position on release. Notes snap to grid with no collision nudge.
- Group drag (multi-select, N>1): dragging any selected node moves the whole group (xyflow shifts every selected node by the same delta, writing their live positions into `nodes` via `onNodesChange`). `commitGroupMove` reads those final positions back from xyflow's store (`flowInstance.getNodes()` — authoritative and synchronous at dragStop, unlike the React `nodes` state which can lag a frame), snaps each, and PATCHes it via the same optimistic `runOptimistic` + `updateSystemOnServer` loop as the single-node path; the collision nudge (at the tighter `MANUAL_SLOT` gap) runs **only against non-selected systems** so an intra-group overlap never deforms the group. Unchanged snapped positions are skipped. (There is no `onSelectionDragStop` — xyflow's persistent multi-select bounding box is suppressed in CSS, see below, so group drag always flows through `onNodeDragStop`.)
- Connect: `nodesConnectable` is on and `connectionMode={ConnectionMode.Loose}` so any of `SystemNode`'s four side handles can act as either source or target during drag. `onConnect` POSTs a new wormhole connection (default scope `wh`), then applies the server payload. (Rendering of the edge ignores which specific handle the user dragged from — `ConnectionEdge` snaps to whichever pair of sides face each other.) Multiple connections leaving the same node face — whether several wormholes to the same neighbour or to different neighbours — each get their own attachment point on that face (see `ConnectionEdge`'s `useEdgeAnchors`).
- Selection: two cooperating pieces of state — `selected: SelectionRef = { kind: 'system' | 'connection' | 'note', id } | null` is the **primary anchor** that feeds `InspectorModule` and every sidebar module (route / intel / structure / system notes / stats / graph / killboard / tags), and `selectedSystemIds: Set<string>` is the multi-select group. Invariant: when `selected?.kind === 'system'` the set contains `selected.id`. **The primary is cleared (`selected = null`) whenever 2+ systems are selected** — a multi-select group drives no inspector / per-system module, which avoids thrashing the self-fetching Graph / Killboard modules as a box drag sweeps nodes; a lone survivor re-populates it. Selecting a connection or clicking the pane clears the set; `size <= 1` is the single-select regime. Handlers:
  - `onNodeClick` — a **note** node selects singly (`selected = { kind: 'note', id: data.id }`, set cleared; no group semantics); a **system** plain click replaces the set with `{node.id}` and sets it primary; **Ctrl/Cmd+click** toggles the node in a fresh set, then sets primary to the lone member when `size === 1`, else null.
  - `onEdgeClick` sets the connection primary and clears the set; `onPaneClick` clears both.
  - `onSelectionChange` is a **box-select-only reconciler**, gated by a `boxSelecting` ref that is true only between `onSelectionStart` and `onSelectionEnd` (the drag-box bracket). xyflow fires `onSelectionChange` for *every* selection mutation, including the echoes of our own click handlers — adopting those would fight the click handlers and loop (Max update depth). So it adopts xyflow's reported node set only while a box drag is active, then **filters to `type === 'system'` nodes** (notes share the node surface but select singly into the inspector — they must never enter `selectedSystemIds`, or a group "Remove N" would route a `note:<id>` through the systems DELETE endpoint and 400), and only when the resulting system set is `size > 1` and differs from `selectedSystemIds` (the diff check skips a no-op rebuild). On adopt it clears the primary (same multi-select rule above).
  - `onSelectionEnd` (fires once when the box drag completes) enforces that **mixed multi-selection is unsupported**: if the final box caught both systems and notes, it prioritizes systems and force-deselects the caught notes in xyflow (`setNodes`, clearing their `selected` flag) so they don't group-drag and rubber-band on release. A pure-note box is left intact (the multi-note drag reads xyflow's selection directly); a pure-system box is unaffected. Done at box end rather than in `onSelectionChange` so it never fights xyflow's per-move re-selection while the rectangle is still being drawn.
  - ReactFlow props enabling the above: `selectionKeyCode`/`multiSelectionKeyCode` `= ['Control', 'Meta']` (Ctrl/Cmd+drag draws a selection box and Ctrl/Cmd+click multi-selects; plain drag still pans), `selectionMode={SelectionMode.Partial}` (touched, not only enclosed), `deleteKeyCode={null}` (xyflow's built-in delete is disabled so a stray Delete/Backspace never wipes systems or edges — there is intentionally no delete keybind at all).
  - The `selected` flag is reflected back into each xyflow node (`selected: selectedSystemIds.has(s.id)`) / edge (from the primary ref) so rebuilding the arrays on a `viewData` change (e.g. an optimistic patch or realtime rebuild) does not wipe selection.
  - A box drag ends up **identical to Ctrl+click**: the same controlled `selected` flags highlight the nodes, and xyflow's persistent bounding box (`.react-flow__nodesselection`) is hidden via a global CSS rule (`src/app/globals.css`) so there is no tight-box overlay and group drag flows through `onNodeDragStop`. The live drag rectangle (`.react-flow__selection`) is a different element and stays visible during the drag.
- Group delete: a `removeSelectedSystems` callback loops the existing single-item `removeSystemOnServer` over the **`deletableSelectedSystemIds` memo** (each via `runOptimistic`), then clears selection — the `onBulkPaste` precedent, no batch endpoint. `deletableSelectedSystemIds` is `selectedSystemIds` minus the Home system (`viewData.map.homeMapSystemId`) and any locked system (`s.locked`): the server rejects deleting those anyway (Home with a "Cannot remove the Home system…" toast, locked outright), so exempting them client-side avoids the delete-then-reappear flicker when a multi-selection accidentally sweeps them in. Triggered **only** by a floating destructive "Remove N" button (there is deliberately **no** Delete/Backspace keybind — a stray backspace while an input is unfocused must never delete systems) (`Trash2` icon, `nodrag nopan`, top-right of the canvas, `z-10`) whose **N is the deletable count** (`deletableSelectedSystemIds.length`); when the selection also includes locked systems the button appends a `(<n> locked)` note and carries a `title` of "<n> locked system(s) excluded — unlock to remove" (count from the `lockedSelectedCount` memo); rendered only when `selectedSystemIds.size > 1` **and** `deletableSelectedSystemIds.length > 0` (so a selection of only protected systems shows no button) **and** no `subchainPreview` **and** no `disconnectedPreview` is pending (those prompts carry their own destructive button, so the redundant "Remove N" is suppressed while either is up even though the doomed set is selected).
- Right-click context menu: `onNodeContextMenu` / `onEdgeContextMenu` / `onPaneContextMenu` each `preventDefault()` the native browser menu and set `contextMenu` to a `MapContextMenuTarget` (`{ kind, id?, x, y }`) from the cursor's client coords. **Selection is left untouched** — the menu operates on its own `id`. Rendered by `<MapContextMenu>` (a sibling of `<ReactFlow>` inside the canvas wrapper); it closes via `onClose` → `setContextMenu(null)` (outside click / Escape / item select). `MapCanvas` feeds it the live target rows (`systems={viewData.systems}`, `connections={viewData.connections}`), the map's `homeMapSystemId`, the current `selectedSystemIds`, and the mutation callbacks it needs: `onSystemPatch`, `onSystemRemove`, `onSystemRemoveSelected` (wired to `removeSelectedSystems` so "Remove from map" deletes the whole group when the right-clicked system is in a multi-selection), `onConnectionPatch`, `onConnectionDelete` (the same optimistic inspector callbacks), `onAddSystemAt` for the pane "Add system" action, `onDeleteSubchain` / `onDeleteSubchainPick` for the delete-subchain entry, `onDeleteDisconnected` for the pane "Delete disconnected" action, `onPingSystem` for the system "Ping" item, plus the note surface: `notes={viewData.notes}`, `onAddNoteAt` (pane "Add note here"), `onNotePatch` (severity / locked), and `onNoteRemove` (Delete). The menu resolves its own target row from those arrays and calls the matching callback per leaf item.
- Connection endpoints: each edge's data carries a stable `onEndpointContextMenu(connectionId, end, clientX, clientY)` callback (built with `useCallback([])`), fed into the connection-edge `useMemo`'s dep array. Right-clicking a `ConnectionEndpoint` dot sets `contextMenu` to a `{ kind: 'connectionEnd', id, end, x, y }` target, opening `MapContextMenu`'s dedicated per-end "Bubbled at …" item rather than the line's full connection menu.
- Delete subchain: a right-click "Delete subchain" entry on a system (`MapContextMenu`) deletes a head system plus its branch in one action. `onDeleteSubchain(headId)` (Home set; Home is the keep-side anchor) and `onDeleteSubchainPick(headId, anchorId)` (no Home; the picked neighbour is the keep-side) both funnel into `openSubchainPreview`, which runs the pure `computeSubchain` (`@/lib/map/subchainGraph`) over the live `viewData` to resolve the doomed set (head + everything orphaned from the anchor), **highlights it** by replacing `selectedSystemIds`, clears the primary, and opens the non-blocking `SubchainDeletePrompt` (`subchainPreview` state, `lead="Delete subchain beyond"`, bottom-left) with the head name + count; the canvas highlight is the visual indication of the doomed set (the "Remove N" button is suppressed meanwhile). Confirm (`onConfirmSubchain`) POSTs `deleteSubchainOnServer` (the server recomputes authoritatively; it ignores the passed anchor when a Home is set) and folds the returned N payloads via `onBulkPaste`; dismiss (`onCancelSubchain`) just clears the preview + highlight. Both clear `selectedSystemIds` when done.
- Delete disconnected: a right-click pane "Delete disconnected" entry (`MapContextMenu`, shown only when a Home is set and ≥1 system is cut off) removes every system with no path back to the Home in one action. `onDeleteDisconnected` runs the pure `computeDisconnected` (`@/lib/map/subchainGraph`) over the live `viewData`, **highlights** the doomed set via `selectedSystemIds`, clears the primary, and opens the non-blocking `SubchainDeletePrompt` (`disconnectedPreview` state, `lead="Delete systems disconnected from Home"`, **no** `headName`, bottom-left) with the count. Confirm (`onConfirmDisconnected`) POSTs the body-less `deleteDisconnectedOnServer` (the server recomputes authoritatively) and folds the returned N payloads via `onBulkPaste`; dismiss (`onCancelDisconnected`) clears the preview + highlight. Both clear `selectedSystemIds` when done.
- Alias and tag: the system tile's `InlineTextEdit` calls back into `MapCanvas`'s `onSystemPatch` via the per-node `onAliasOrTagCommit` injected through `data`.
- Home marker: each node's `data.isHome` is derived in both node builders (initial + the `viewData` sync) as `s.id === viewData.map.homeMapSystemId`, so the designated Home tile renders its accent ring + icon (see `SystemNode`). Home is a load-time map property (not realtime), so it only changes on a fresh map load.
- **Command palette + hotkeys**: a `CommandPalette` (⌘K/Ctrl-K or `/`) and a `MapHotkeys` layer are mounted beside `SignaturePasteHotkey`, both fed the same memoized `KeyboardActionContext` — the selected system/connection plus the exact mutation callbacks the buttons use (`onSystemPatch`/`onSystemRemove`/`onConnectionPatch`/`onConnectionDelete`), `openAddSystem`, and the jump-to-system focus handler. `onMoveSelection(dir)` moves the selection to the nearest graph-adjacent system within a direction cone (falling back to nearest by position; nothing selected starts at Home), reusing the jump handler's select + snap-center; `onClearSelection` backs Esc.
- Read-side data backfill: `intel` / `stats` / `structures` / `systemNotes` are **state** seeded from the load-time props. The page server-renders them only for the systems present at load, so a `useEffect` keyed on `viewData.systems` backfills systems added live (paste, tracked-pilot jump, manual add): it diffs each system's `systemId` against a `requestedSystemData` ref (seeded from the load-time intel keys), batch-fetches the missing ids via `fetchSystemData` (one `GET /api/map/[mapId]/system-data`), and merges `{ intel, stats, structures, systemNotes }` into state. Additive only — an id already requested is never refetched (a stale snapshot matches the prior load-time model); a failed fetch un-marks its ids so the next system change retries.
- Read-side periodic refresh: a second `useEffect` (armed once per map) re-pulls `fetchSystemData` for the **whole** current on-map set every `SYSTEM_DATA_REFRESH_MS` (~5 min, the server refresh-job floor) and reference-replaces `intel` / `stats` so incursion/sov/FW decorators and the intel/stats modules catch mid-session drift without a reload. Reads the live system id set from an `onMapSystemIdsRef` mirror so the interval never re-arms on a `viewData` change; **skips the fetch while `document.visibilityState !== 'visible'`**. Distinct from the one-shot backfill (which handles brand-new ids instantly); the two compose — the refresh overwrites, the backfill fills gaps. `structures` / `systemNotes` are **not** re-merged here: they carry the viewer's own un-echoed CRUD edits and have no server refresh job.
- Intel decorators: each node's `data.inFactionWarfare` / `data.hasIncursion` are derived in both node builders from the `intel` **state** (`intel[s.systemId]?.factionWar != null` / `?.incursion != null`), driving `SystemNode`'s bottom-right FW/incursion pills. Because `intel` is replaced by reference when a backfill lands, it **is** part of the node-sync key (alongside `viewData.systems` / `selectedSystemIds`) — so a live-added system's decorators appear as soon as its data arrives, without waiting for a further systems change.
- Structure intel: `structures` is local state seeded from the page load and **also** backfilled for live-added systems by the read-side effect above. `onStructureCreate`/`onStructurePatch`/`onStructureDelete` await the `@/lib/structures/client` REST wrappers and fold the returned `StructureIntel` into local state on success. These are **not** map events and have no realtime echo (structures are deployment-global, not map-scoped), so there is no `applyEvent` / `appliedEventIds` involvement and another user's edits to an already-loaded system appear only on the next page load.
- Global system notes: `systemNotes` follows the same model — local state seeded from the page load, backfilled by the read-side effect, mutated by `onSystemNoteCreate`/`onSystemNotePatch`/`onSystemNoteDelete` via the `@/lib/system-notes/client` REST wrappers (lists kept newest-first; the patch callback passes any `UpdateSystemNoteBody` through, so the module's lock toggle and dialog edits share it). Deployment-global, no realtime echo. `systemNotes` also feeds each system node's `hasNotes` flag (the cyan notebook indicator) and is part of the node sync key, so the indicator tracks the viewer's own CRUD and the backfill. `onJumpToSystem(systemId)` (wired to the module's notes browser) focuses a visible on-map system — selects it and snap-centers, like the sig-search jump — or toasts "not on this map".
- Toasts: client helpers in `@/lib/map/client` surface server errors as `toast.error` before returning the failure result; `MapCanvas` handles rollback and the `resync()` failsafe. `fetchMapSnapshot` is the exception — it uses a bare fetch (no toast) so a failed resync doesn't double-toast on top of the originating mutation's error.
- Add system (manual): the pane context-menu "Add system" item opens `AddSystemDialog`. Selecting a search result fires `onAddSystem(systemId)`, which POSTs via `addSystemOnServer` and folds the returned `payloads` through `onBulkPaste` — the add returns the `system.added` event **plus any auto-created `stargate` gate links** to systems already on the map (K-space/Pochven; wormhole adds return a single payload). Placement first honours a pending cursor point: the pane context-menu "Add system" item calls `onAddSystemAt(clientX, clientY)`, which stores the client coords in the `pendingAddPoint` ref, closes the menu, and opens the dialog; `onAddSystem` then converts that point via `flowInstance.screenToFlowPosition` and clears the ref. Absent a pending point it anchors on the selected system's position when `selected.kind === 'system'`, else the current viewport centre (from the `ReactFlowInstance` captured in `onInit` + the canvas wrapper's bounding rect, falling back to (0,0) before the instance is ready), then `findOpenPosition(anchor, occupied)` (`@/lib/map/placement`, `occupied` derived from `viewData.systems`) settles it into the nearest open, grid-aligned slot so adds never overlap existing nodes. This is the "add a system without jumping a wormhole" pathway — no connection is created.
- Map info: the toolbar "Map info" button toggles `mapInfoOpen`, opening `MapInfoDialog` (Summary/Systems/Connections) with the live `viewData` — no fetch.
- Search: the `sigSearch` panel renders `SignatureSearchModule` (via `panelContent`) with the live `viewData.signatures` and `viewData.systems` and the live `sigSearchFilters`. On result click it calls `handleNavigateToSig`, which sets the selection to the target system, **snaps** the viewport to centre it at the current zoom (`setCenter`, `duration: 0` — no animated move/zoom), sets `flashSigId` to flash the signature for 3s, then clears it (the panel stays open). It is a registered dashboard panel — managed via the Panels dropdown, not a button.
- Pilots: the toolbar `PilotRosterButton` opens the online-pilot roster in a non-blocking popover (map stays interactive), reading the roster from the presence store. Both it and the dialog live inside `MapPresenceProvider` so `usePresenceForMap` resolves.
- Overlay: the toolbar `SystemOverlayButton` pops the read-only `SystemOverlay` into an always-on-top Document Picture-in-Picture window (Chromium-only; disabled with a tooltip otherwise). It sits inside both presence + active-char providers, so the portalled overlay reads the same live `viewData`/presence/active-char state with no extra data wiring.
- Settings: the toolbar "Settings" button toggles `settingsOpen`, opening `MapSettingsDialog` seeded with the `settings` prop. Its import flow returns committed event payloads which are folded onto the canvas via the same `onBulkPaste` handler used by signature paste (registers each `eventId`, applies via `applyEvent`).

### Emits / Calls
- `onNodesChange` — xyflow callback; applies `NodeChange[]` to the nodes state via `applyNodeChanges`.
- `useMapSubscription` / `useRealtimeEvents` / `useReconnectResync` — subscribe to the map channel + consume live `mapUpdate` envelopes (every envelope delivered once, no coalescing) + refetch the snapshot on socket reconnect.
- `applyEvent` — pure reducer applied for every event and every optimistic patch.
- `@/lib/map/client` — all the mutation wrappers (system / connection / signature / subchain / disconnected) + `fetchWormholeTypes` (via the inspector) + `fetchSystemData` (the read-side backfill for live-added systems) + `fetchMapSnapshot` (via the `resync()` failsafe).

- Wraps the canvas subtree in `MapPresenceProvider` seeded from `data.presence` (`MapPresenceContext`) so each `SystemNode` can pull its system's online-pilot slice via `usePresenceForSystem` without prop-drilling. The provider also folds incoming `characterUpdate` envelopes onto that store.
- Inside that, wraps the subtree in `MapActiveCharProvider` (passing `viewerCharacters` and `mainCharacterId`) so the `ActiveCharSelector` toolbar element and `RoutePlannerModule` can read the active character via `useMapActiveChar()`. The provider builds `locatedChars` (only characters in the presence roster) and manages `activeCharId` (user pick → main → first located).
- Inside that, wraps the subtree in `MapTravelProvider` (`MapTravelContext`) so `ConnectionEdge` can read its own travel pulse. When `travelAnimation` is true, mounts `TravelBridge` (passing `viewData.systems` + `viewData.connections`) which listens to presence traversals and pulses the matching edge; when false the bridge is absent so no animation ever plays.
- Inside that, wraps the subtree in `MapUnderglowProvider` (`MapUnderglowContext`) so each `SystemNode` can read its own underglow slice, and always mounts `MapUnderglowBridge` (passing `viewData.systems`) which turns incoming `systemNotification` realtime events into a per-node glow pulse (kind `killmail` from the zKB feed, kind `ping` from a user ping).
- Inside that, wraps the subtree in `MapSignatureIndicatorProvider` (`MapSignatureIndicatorContext`) — fed `viewData.signatures` + `signatureIndicators` — so each `SystemNode` reads its stale/unscanned indicator slice via `useSignatureIndicator`. Re-seeds when the signature array changes (realtime/optimistic) and ticks every 60s for the stale clock.
- Ping: `onPingSystem(mapSystemId)` (wired to the `MapContextMenu` "Ping" item) is a fire-and-forget `pingSystemOnServer` call — **no** optimistic apply. The server broadcasts a `systemNotification` (kind `ping`) to all viewers; this client receives its own echo and renders the pulse through `MapUnderglowBridge` like everyone else.
- Mounts `SignaturePasteHotkey` (inside `MapPresenceProvider`, so it can read live presence) — the CTRL+V fast-paste listener. On a scanner-data paste it applies add+update sigs straight to `selectedSystem` via `onBulkPaste`, gating behind a mismatch confirm when none of `viewerCharacterIds` are located in the selected system. Owns the one-shot `lazyDeleteSigs` state shared between the `SignatureModuleHeaderActions` toggle (`lazyDelete` / `onLazyDeleteChange`) and the hotkey (`lazyDelete` / `onLazyDeleteConsume`): when armed, the next committed direct paste also removes missing sigs, then disarms. The armed paste routes its result through `onLazyDeletePasteResult` (instead of `onBulkPaste`), which raises the same subchain-delete prompt the row trash icon does for every removed wormhole sig — see `onSignatureDelete` below.
- Mounts `TransitSignaturePrompt` inside the canvas wrapper (sibling of the "Remove N" button). It watches `useTraversals` for the viewer's own pilots (`viewerCharacterIds`); when one transits a non-gated hole it offers the source system's matching wormhole sigs (top-left overlay) and, on click, populates that sig's "Leads to" via `onSignaturePatch`. Silent for gate jumps, filaments, and unscanned sources.
- `onSignatureDelete(signatureId)` fires the optimistic sig delete, then enqueues a subchain offer via the shared `buildSubchainSigOffer(sig)` helper — if the sig had a populated "Leads to" (`mapConnectionId`), it computes the subchain behind that hole (head = the connection's far end; anchor = Home if set, else the sig's own system) and, when non-empty, returns the offer that's pushed onto `subchainSigPrompts`. `buildSubchainSigOffer` reads the **pre-removal** graph, so callers invoke it before folding the delete into `viewData`. The active offer (`subchainSigPrompts[0]`) drives the non-blocking `SubchainDeletePrompt` (bottom-left overlay). Confirm (`onConfirmSubchainSig`) runs `deleteSubchainOnServer` + `onBulkPaste` and pops the queue; dismiss (`dismissSubchainSig`) just pops. It deliberately does **not** highlight the doomed set (that would surface the "Remove N" button).
- `onLazyDeletePasteResult(payloads)` is the result handler for a lazy-delete paste (passed to `SignaturePasteHotkey` in place of `onBulkPaste` when the lazy-delete arm is set). It builds a subchain offer (via `buildSubchainSigOffer`, against the pre-fold graph) for each `signature.delete` payload, also builds restore offers (`buildRestoreOffers`), folds the paste through `onBulkPaste`, then enqueues the subchain offers onto `subchainSigPrompts` and the restore offers onto `restoreConnPrompts`.
- `onSignaturePasteResult(payloads)` is the **non-lazy** signature-paste result handler, wired to both signature-paste entry points (the `SignaturePasteHotkey` `onBulkPaste` prop and the `SignatureModuleHeaderActions` paste dialog) in place of the raw `onBulkPaste`. It builds restore offers from the committed payloads (pre-fold), folds via `onBulkPaste`, then enqueues onto `restoreConnPrompts`. (`onBulkPaste` is still used directly by the non-signature consumers: Thera sync, import, subchain delete, manual add.)
- `buildRestoreOffers(payloads)` scans committed paste payloads for re-confirmed wormhole sigs whose remembered connection is **dormant** — reading the sig body from `payload.snapshot` (a re-pasted surviving sig's `signature.update`) or the `signature.create` payload itself, then offering when `groupKey === 'wormhole'`, `mapConnectionId != null`, and that id is **absent** from `viewData.connections` (absent ⇒ dormant/hidden). `targetName` resolves from `viewData.systems` by the snapshot's `leadsToMapSystemId` (alias/name), falling back to the WH code or "wormhole" when the far system is itself hidden. De-duped by connection id; multiple wh sigs → multiple offers. The active offer (`restoreConnPrompts[0]`) drives a `RestoreConnectionPrompt` (bottom-right overlay); confirm (`onConfirmRestoreConn`) runs `restoreConnectionOnServer` + `onBulkPaste` and pops the queue; dismiss (`dismissRestoreConn`) just pops.
- Threads `viewData.connections` and `viewData.systems` into `SignatureModule` so its `ConnectionSelect` can list connections incident to the active system without an API call. Also passes `onConnectionPatch` so the module can auto-set a linked connection's jump-mass size when a WH sig's type + leads-to are both known. Passes `flashSigId` so the module can flash the signature for 3s after a search navigation, and `pasteFlash` so it can sonar-ping rows a local bulk paste created/updated.
- `panelHeaderRight(id)` mirrors `panelContent(id)` for `MapPanelGroup`'s header slot (rendered for the active tab). For the `signatures` panel it returns `SignatureModuleHeaderActions` (the **Lazy delete** + **Paste from scanner** buttons plus **Search**) so they sit in the panel header beside the tabs/drag-handle rather than inside the card; all other panels return `undefined`.

### Depends On
- `@xyflow/react`, `./SystemNode`, `./MapNoteNode`, `./ConnectionEdge`, `./MapContextMenu`, `./SubchainDeletePrompt`, `./RestoreConnectionPrompt`, `./MapPresenceContext`, `./MapActiveCharContext` (`MapActiveCharProvider`, `useMapActiveChar`), `./MapTravelContext`, `./MapUnderglowContext`, `./MapUnderglowBridge`, `./SignaturePasteHotkey`, `./TransitSignaturePrompt`
- `./ChainTabStrip`, `./ChainCanvas` (`ChainCanvas`, `CHAIN_TILE_PARAMS`, `ChainCanvasNode`, `ChainFocusRequest`), `./ChainPointerNode` (`ChainPointerNodeData`) — the chain-mode render path.
- `@/lib/map/chains/view` — `buildChainCanvas` (pure viewData → chain-canvas derivation), `sortChainsForTabs`.
- `./layout/MapLayoutGrid` (`MapLayoutGrid`), `./layout/MapPanelGroup` (`MapPanelGroup`) — the free-form grid + per-cell tabbed chrome.
- `./layout/PanelDndContext` (`PanelDndContext`) — dnd-kit wrapper around the grid that drives tab merge/reorder via `handlePanelDrop`.
- `@dnd-kit/sortable` — `arrayMove` for `reorderTab`.
- `@/lib/map/layout/panels` — `PANELS` (registry), `PANEL_COLS` / `PANEL_MIN` (per-breakpoint cols + per-panel resize floors, for `tearOffTab`'s new-cell sizing/clamping), `DEFAULT_MAP_LAYOUT` (fallback arrangement), `migrateLayout` (pre-v2 → singleton groups) + `dedupeGroups` (heal duplicated tabbed members) + `ensurePanelsPlaced` (forward-compat auto-place of newly shipped and re-shown panels) when seeding layout state, on import, and on show; `removePanelFromLayout` (group-aware hide) on hide.
- `@/lib/map/layout/schema` — `mapLayoutConfigSchema`, the Zod validator re-used client-side to vet an imported layout file before applying it.
- `setMapLayoutAction` (`@/app/(app)/actions/account`) — debounced persistence of the layout.
- `sonner` — `toast` for import success / failure feedback.
- `react-grid-layout` — `Layout` / `ResponsiveLayouts` types for `handleLayoutChange`/`mergeLayouts`.
- `@/lib/map/subchainGraph` — `computeSubchain` for the client-side delete-subchain preview/highlight; `computeDisconnected` for the delete-disconnected preview/highlight.
- `@/components/dialogs/MapInfoDialog`, `@/components/dialogs/MapSettingsDialog`, `./AddSystemDialog`, `./PilotRosterButton`, `./SystemOverlayButton`, `./MapShareIndicator`, `@/components/ui/button`
- `RoutePlannerModule`, `KillStatsModule`, `InspectorModule`, `SignatureModule`, `SignatureSearchModule`
- `IntelModule`, `StructureModule`, `SystemNotesModule`, `SystemGraphModule`, `SystemKillboardModule` (the latter two self-fetch per-selected-system from `/api/system/[id]/…`)
- `TagsModule`, `TheraModule` (global, always-on; `TheraModule` self-fetches `/api/map/[id]/thera` and folds synced edges via `onBulkPaste`)
- Structure REST wrappers in `@/lib/structures/client`
- System-note REST wrappers in `@/lib/system-notes/client`
- `applyEvent` (`@/lib/map/applyEvent`)
- `@/lib/map/placement` — `GRID_SIZE`, `MANUAL_SLOT`, `findOpenPosition`, `overlaps`, `snapToGrid` (aliased `snapPointToGrid` to avoid clashing with the ReactFlow `snapToGrid` prop) for manual-add anchoring (default auto gap), drag nudge (`MANUAL_SLOT` gap), and live grid snap.
- `mapUpdateLoadSchema`, `Envelope` (`@/lib/realtime/protocol`)
- `useMapSubscription`, `useRealtimeEvents`, `useReconnectResync` (`@/lib/realtime/useRealtime`)
- All mutation wrappers in `@/lib/map/client` (incl. `createChainOnServer` / `renameChainOnServer` / `deleteChainOnServer`)
- `lucide-react` — `User` icon for the character selector

### Local State
- `selected: SelectionRef | null` — primary (anchor) selection; drives the inspector and all sidebar modules.
- `selectedSystemIds: Set<string>` — the multi-select group (always replaced, never mutated). `size <= 1` is the single-select regime. Drives each node's `selected` flag and the floating "Remove N" button.
- `contextMenu: MapContextMenuTarget | null` — the active right-click target (kind + id + cursor x/y); `null` ⇒ no menu open. Independent of `selected`/`selectedSystemIds`.
- `subchainPreview: { headId, anchorId, headName, count } | null` — the pending context-menu delete-subchain confirmation (drives a `SubchainDeletePrompt` with `lead="Delete subchain beyond"`); the doomed systems are simultaneously highlighted via `selectedSystemIds`. `null` ⇒ no prompt.
- `subchainSigPrompts: SubchainSigOffer[]` — FIFO queue of pending non-blocking "also delete the subchain?" offers raised after deleting "Leads to" wormhole sigs (`[0]` drives a `SubchainDeletePrompt`). The row trash icon enqueues one; a lazy-delete paste can enqueue several. No canvas highlight. Empty ⇒ no prompt.
- `restoreConnPrompts: RestoreConnOffer[]` — FIFO queue of pending non-blocking "restore connection to X?" offers (`{ connId, targetName }`) raised by `buildRestoreOffers` when a paste re-confirms a wormhole sig whose remembered connection is dormant (`[0]` drives a `RestoreConnectionPrompt`, bottom-right). One per dormant connection; multiple wh sigs in one paste enqueue several. Empty ⇒ no prompt.
- `disconnectedPreview: { count } | null` — the pending pane delete-disconnected confirmation (drives a `SubchainDeletePrompt` with `lead="Delete systems disconnected from Home"`, no `headName`); the doomed systems are simultaneously highlighted via `selectedSystemIds`. `null` ⇒ no prompt.
- `mapInfoOpen: boolean` — whether the `MapInfoDialog` is open.
- `settingsOpen: boolean` — whether the `MapSettingsDialog` is open.
- `addSystemOpen: boolean` — whether the `AddSystemDialog` is open.
- `sigSearchFilters: SigSearchFilters` — the live filter state for the `sigSearch` panel's `SignatureSearchModule` (name, groupKey, maxAgeHours, securityClasses, includeAnomalies, includeSignatures).
- `flashSigId: string | null` — a signature id to highlight in the signature panel for 3s after search navigation; clears itself via a setTimeout when set.
- `pasteFlash: Record<string, 'created' | 'updated'>` — sig ids a local bulk paste just created/updated, mapped to which; drives the signature-panel sonar-ping highlight (green/blue). Set by `onBulkPaste`, cleared after 2.5s.
- `flowInstance: ReactFlowInstance | null` (ref) — captured in `onInit`; used to map screen → flow coords for manual system placement.
- `flowWrapperRef: HTMLDivElement | null` (ref) — the canvas wrapper, for its bounding rect (viewport centre).
- `pendingAddPoint: { x, y } | null` (ref) — client-space cursor point from the pane "Add system" action; consumed and cleared by the next `onAddSystem` so the node lands where the user right-clicked. (Notes need no equivalent — "Add note here" POSTs immediately from the click point.)
- `boxSelecting: boolean` (ref) — true only during a drag-box selection (set by `onSelectionStart`/`onSelectionEnd`); gates `onSelectionChange` so it adopts box selections but ignores click-driven echoes.
- `viewData: MapViewData` — mutable canvas state; updated by realtime events + optimistic patches.
- `liveShares: LiveShareBadge[]` — share links publishing this map; seeded from the prop, updated by `share.created` / `share.revoked` events.
- `nodes: CanvasNode[]` (`Node<SystemNodeData> | Node<MapNoteNodeData>`) — xyflow's controlled nodes state; mutated by `applyNodeChanges` (drag, measure, selection) and reconciled from `viewData.systems` + `viewData.notes` via the render-time sync block. Each node's `draggable` is set from `!locked`. Note nodes use namespaced ids (`note:<id>`) to avoid colliding with system ids.
- `appliedEventIds: Set<number>` (ref) — dedup set for realtime event ids and committed optimistic eventIds. Cleared by `resync()` when a fresh snapshot replaces `viewData`.
- `resyncInFlight: boolean` (ref) — guards `resync()` so a burst of mutation failures triggers at most one in-flight snapshot refetch.
- `initialViewport: Viewport | null` — lazy-init from `localStorage`; null triggers `fitView`, non-null restores the saved pan/zoom position. `lastViewportRef` (ref) tracks the latest free-canvas viewport (`onMoveEnd`) so a remount after a chain-tab visit restores where the user left off.
- `chainView: { activeChainId: string | null; orientation: ChainLayoutOrientation }` — the chain-mode display preference (active tab; null = All/free canvas; tree orientation), lazy-init from and persisted to `localStorage` (`aperture:map:<id>:chainView`) via `updateChainView`.
- `chainFocus: ChainFocusRequest | null` — one-shot "center the chain canvas on this system" request (token-bumped by `requestChainFocus`); consumed by `ChainCanvas`.
- `activeChainIdRef` (ref) — effect-mirrored active chain id so mode-agnostic callbacks (jump-to-system, sig search) branch without re-memoizing.
- `layout: MapLayoutConfig` — the live dashboard arrangement; seeded from the `mapLayout` prop (or `DEFAULT_MAP_LAYOUT`) via `ensurePanelsPlaced` (auto-places any panel missing from a saved layout), updated on grid drag/resize, per-panel hide/show (✕ button + Panels dropdown), Reset, and Import (file), then debounce-persisted via `setMapLayoutAction`. `saveTimer` (ref) holds the pending debounce; `firstLayoutChange` (ref) suppresses the save on RGL's mount-time `onLayoutChange`. `importInputRef` (ref) is the hidden file picker driving "Import layout".
- `intel: Record<number, SystemIntelSummary>` — read-side sov/FW/incursion/EVE-Scout intel keyed by EVE system id; seeded from the `intel` prop and backfilled for live-added systems via `fetchSystemData`. Part of the node-sync key.
- `stats: Record<number, SystemStatsSummary>` — rolling 24h activity totals keyed by EVE system id; seeded from the `stats` prop and backfilled for live-added systems via `fetchSystemData`.
- `structures: Record<number, StructureIntel[]>` — manual structure intel keyed by EVE system id; seeded from the `structures` prop, backfilled for live-added systems via `fetchSystemData`, and updated on the user's own structure CRUD (no realtime).
- `systemNotes: Record<number, SystemNote[]>` — global system notes keyed by EVE system id; same seed/backfill/CRUD model as `structures`.
- `requestedSystemData: Set<number>` (ref) — EVE system ids whose read-side data is loaded or in flight; seeded from the load-time intel keys. Gates the backfill effect so each id is fetched at most once (failed fetches un-mark for retry).
- `sigHydrateInFlight: Set<string>` (ref) — `ap_map_system.id`s with an in-flight signature-hydration fetch; dedupes concurrent `system.added` hydrations for the same system (cleared on completion, so a later re-add refetches).
